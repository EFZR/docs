# 🗃️ System Migration: NixOS ➡️ Arch Linux

## 📦 Main Development Tools

* **Neovim** (`nvim`)
* **Docker**
* **Node.js (via NVM)**
* **.NET SDK** (`dotnet`)
* **Rust toolchain**
* **Zig compiler**
* **Firefox browser**

---

## 🖥️ Terminal & Shell Tools

* **Zsh** – Shell of choice
* **Oh My Zsh**
* **Tmux** – Terminal multiplexer
* **Ghostty** – GPU-accelerated terminal emulator

---

## 🧰 CLI Utilities

| Tool                 | Purpose                                    |
| -------------------- | ------------------------------------------ |
| `bat`                | A `cat` clone with syntax highlighting     |
| `direnv`             | Auto-set environment variables per project |
| `eza`                | Improved `ls` replacement                  |
| `fzf`                | Fuzzy finder                               |
| `ripgrep`            | Fast search tool (like `grep`)             |
| `fd`                 | Faster `find` alternative                  |
| `jq`                 | JSON parser                                |
| `btop`               | Modern system monitor                      |
| `wget`               | Command-line downloader                    |
| `gnumake`            | Build automation tool                      |
| `gcc`                | GNU C compiler                             |
| `cmake`              | Cross-platform build system                |
| `cmatrix`            | Matrix-style terminal display              |
| `zip`, `unzip`, `xz` | Compression utilities                      |
| `fastfetch`          | Neofetch-style system info                 |
| `gh`                 | GitHub CLI                                 |
| `git`                | Version control                            |
| `yazi`               | Terminal file manager                      |
| `nmap`               | Network scanner                            |
| `obsidian`           | Markdown-based note-taking                 |
| `obs-studio`         | Streaming and screen recording             |

---

## 🧪 Virtualization Tools

* **QEMU**
* **Virt-Manager**

---

## ✅ Optional but Useful (consider adding)

* **`zoxide`** – Smarter `cd` alternative
* **`lazygit`** – Terminal UI for Git
* **`htop`** – Interactive process viewer (alt to `btop`)
* **`gparted`** – Partition manager (GUI)

---

## 🧰 Work dependencies

* firebase-tools
* ffmpeg
* balena-cli
* mpv

* nodejs:
    * electron (make work wink-devices-2)
    * serve

---

## 📓 Important Things To Do

* Set up **SSH access** with my GitHub account
* Sign in to all my **Google accounts** (personal & work)
* Configure my **web browser** (extensions, settings, sync, etc.)
* Apply all necessary changes to my **Neovim setup**
* Install all **missing dependencies** required for my work projects

